% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_sim_study.R
\name{plot_metric_diffs}
\alias{plot_metric_diffs}
\title{Plot paired metric differences between two methods (base graphics)}
\usage{
plot_metric_diffs(
  df,
  metric_col = "FVU",
  method1 = "method1",
  method2 = "method2",
  bend = 1
)
}
\arguments{
\item{df}{Data-frame that contains at least the columns
\code{method}, \code{fname}, \code{rep} and the chosen metric.}

\item{metric_col}{Name of the metric column (character).  Default
\code{"FVU"}.}

\item{method1}{First method (default \code{"method1"}).}

\item{method2}{Second method (default \code{"method2"}).}

\item{bend}{Bending parameter \eqn{0<p\le 1} for Tukey’s modulus
transform; Default \code{bend = 1} is equivalent to no transform.}
}
\value{
Invisibly returns the data frame of differences used for plotting.
}
\description{
For every test function and replicate the difference
  \eqn{d = \text{metric}_{\text{m1}} - \text{metric}_{\text{m2}}}
is computed.  The per-function average is drawn as a vertical segment
(blue when \code{method1} is better, red otherwise) and individual
replicate differences are shown as jittered points of matching colour.
}
\details{
A signed-log (modulus) transform is applied so large positive/negative
values are compressed while small differences stay legible.  Setting
\code{bend = 1} turns the transform off (ordinary linear axis).
}
