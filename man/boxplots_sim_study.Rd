% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxplots_sim_study.R
\name{boxplots_sim_study}
\alias{boxplots_sim_study}
\title{Boxplots for Emulator Comparison}
\usage{
boxplots_sim_study(
  df,
  metric = "CRPS",
  methods = NULL,
  group_by = NULL,
  path = NULL,
  log_scale_y = FALSE,
  y_limit = NULL,
  show_mean = FALSE,
  notches = FALSE,
  show_outliers = TRUE
)
}
\arguments{
\item{df}{A data frame from \code{run_sim_study()}.}

\item{metric}{Character. The metric to plot (e.g., \code{"CRPS"}, \code{"RMSE"}, \code{"t_tot"}).}

\item{methods}{Optional. A character vector of methods to include.}

\item{group_by}{Optional. A character vector of grouping columns (e.g., \code{"fname"}, \code{"NSR"}).}

\item{path}{Optional file path to save the figure. If NULL, returns a ggplot object.}

\item{log_scale_y}{Logical. Should the y-axis be log-scaled? Default is \code{FALSE}.}

\item{y_limit}{Optional. A numeric vector of length 2 to set y-axis limits.}

\item{show_mean}{Logical. Add a point for the group mean? Default is \code{FALSE}.}

\item{notches}{Logical. Should boxplot notches be shown? Default is \code{FALSE}.}

\item{show_outliers}{Logical. Show boxplot outliers? Default is \code{TRUE}.}
}
\value{
A ggplot object or saves a figure to \code{path}.
}
\description{
Create side-by-side boxplots of a performance metric across methods.
}
\details{
This function is primarily intended for visualizing filtered subsets of simulation output
(see \code{filter_sim_study()}). If multiple functions are present without grouping,
a warning is issued. Faceting by group is supported, but more than 8 groups may produce an unreadable plot.
}
\examples{
\dontrun{
filtered <- filter_sim_study(results, n_train = 1000, NSR = 0)
boxplots_sim_study(filtered, metric = "CRPS")

boxplots_sim_study(filtered, metric = "t_tot", log_scale_y = TRUE, show_mean = TRUE)

boxplots_sim_study(results, metric = "CRPS", group_by = c("fname", "NSR"))
}
}
