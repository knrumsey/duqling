% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rankplot_sim_study.R
\name{rankplot_sim_study}
\alias{rankplot_sim_study}
\title{Rank Plot for Emulator Performance}
\usage{
rankplot_sim_study(
  df,
  metric = "CRPS",
  methods = NULL,
  ties_method = "min",
  use_shapes = TRUE,
  path = NULL
)
}
\arguments{
\item{df}{A data frame (typically from \code{run_sim_study()} or aggregated results).}

\item{metric}{Character. The name of the metric to rank by (e.g., "CRPS", "RMSE", "t_tot").}

\item{methods}{Optional character vector of method names to include. Default is all.}

\item{ties_method}{How to handle ties in ranking (passed to \code{rank()}). Default is "min".}

\item{use_shapes}{Logical. Whether to use different point shapes in addition to color. Default is TRUE.}

\item{path}{Optional file path to save plot (e.g., "figs/CRPS_results.png"). If NULL, the plot is returned instead of saved.}
}
\value{
A ggplot object (invisibly), or writes a file if \code{path} is specified.
}
\description{
Create a cumulative rank plot for a given performance metric (e.g., CRPS, RMSE).
}
\examples{
\dontrun{
rankplot_sim_study(results, metric = "CRPS")
rankplot_sim_study(results, metric = "RMSE", methods = c("BART", "GP"), path = "figs/rmse.png")
}
}
