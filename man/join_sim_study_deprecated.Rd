% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_sim_study.R
\name{join_sim_study_deprecated}
\alias{join_sim_study_deprecated}
\title{Join Simulation Study Results}
\usage{
join_sim_study_deprecated(df1, df2, on_mismatch = c("union", "intersect"))
}
\arguments{
\item{df1}{A data frame from \code{run_sim_study()}.}

\item{df2}{Another data frame to join with \code{df1}.}

\item{on_mismatch}{Character string specifying how to handle mismatched columns.
Options are \code{"union"} (default) to include all columns and pad with \code{NA}s as needed,
or \code{"intersect"} to retain only columns common to both data frames.}
}
\value{
A combined data frame.
}
\description{
Joins two simulation study result data frames, allowing for mismatched columns.
}
\examples{
df1 <- data.frame(a = 1:3, b = letters[1:3])
df2 <- data.frame(b = letters[4:5], c = 10:11)

# Keep all columns (union)
join_sim_study(df1, df2, on_mismatch = "union")

# Keep only common columns (intersect)
join_sim_study(df1, df2, on_mismatch = "intersect")

}
