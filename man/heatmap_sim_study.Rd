% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_sim_study.R
\name{heatmap_sim_study}
\alias{heatmap_sim_study}
\title{Heatmap of Method Performance Across Simulation Scenarios}
\usage{
heatmap_sim_study(
  obj,
  metric = "CRPS",
  group_by = "id",
  show_values = NULL,
  color_scale = "turbo",
  orientation = c("vertical", "horizontal"),
  y_scale_fun = NULL,
  colorbar_labels = NULL,
  discrete_colorbar = FALSE,
  title = NULL,
  ...
)
}
\arguments{
\item{obj}{A \code{duq_sim_study} object from \code{process_sim_study()}.}

\item{metric}{Character; the performance metric column to plot (default "CRPS").
Can include derived suffixes like \code{"_rank"}, \code{"_auc"},
\code{"_rel"}, \code{"_rel_log"}, or \code{"_norm"}.}

\item{group_by}{Character vector of columns that define each simulation
scenario group (default = "id").}

\item{show_values}{Logical; if TRUE, overlay numeric values (rounded to 2 decimals).
If NULL (default), values are shown only for small grids (<= 8 × 8).}

\item{color_scale}{Either a single viridis option
("magma","inferno","plasma","viridis","cividis","rocket","mako","turbo"),
or a 2-element character vector of colors for a low-high gradient.
Default = "turbo".}

\item{orientation}{Either "vertical" (methods on x-axis, scenarios on y-axis)
or "horizontal" (scenarios on x-axis, methods on y-axis).}

\item{y_scale_fun}{Optional function applied to aggregated values (e.g., discretizer).}

\item{colorbar_labels}{Optional list with \code{$breaks} and \code{$labels}
for the colorbar.}

\item{discrete_colorbar}{Logical; if TRUE, force discrete colorbar (values converted
to factor). Default = FALSE.}

\item{title}{Optional plot title. If NULL, auto-generate; if FALSE, suppress.}
}
\value{
A \code{ggplot} heatmap.
}
\description{
Visualize the average performance of methods across grouped simulation
scenarios using a heatmap. Supports both rank-based and raw metrics
(and other derived metrics like relative or normalized scores).
}
