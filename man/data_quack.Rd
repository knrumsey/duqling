% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_quack2.R
\name{data_quack}
\alias{data_quack}
\title{Get UQ Dataset Information}
\usage{
data_quack(
  dname = NULL,
  raw = FALSE,
  input_dim = NULL,
  input_cat_dim = NULL,
  n = NULL,
  output_dim = NULL,
  response = NULL,
  sorted = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{dname}{Character vector of dataset names to filter.}

\item{raw}{Logical. When `TRUE`, return information on raw datasets.
Otherwise, return information on emulation-ready datasets.}

\item{input_dim}{Vector of quantitative input dimensions to filter.}

\item{input_cat_dim}{Vector of categorical input dimensions to filter.}

\item{n}{Vector of sample sizes to filter.}

\item{output_dim}{Vector of output dimensions to filter. Used only when
`raw = TRUE`.}

\item{response}{Character string specifying response type for emulation-ready
datasets. Can be `"all"`, `"univariate"`, `"multivariate"`, or
`"functional"`. Backward-compatible aliases such as `response_type = "uni"`
are also accepted.}

\item{sorted}{Logical. Should results be sorted by input dimension and then
alphabetically?}

\item{verbose}{Logical. Print information for a single dataset?}
}
\value{
A data frame of datasets meeting the filter requirements, or an
  invisible named list for a single dataset when `dname` has length 1.
}
\description{
Returns information about datasets available through `duqling`.
}
\examples{
data_quack(raw = TRUE)
data_quack(raw = FALSE)
data_quack(response = "univariate")
data_quack("pbx9501_gold")
}
