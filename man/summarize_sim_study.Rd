% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_sim_study.R
\name{summarize_sim_study}
\alias{summarize_sim_study}
\title{Summarize performance of methods in a duq_sim_study}
\usage{
summarize_sim_study(
  obj,
  summarize = "time",
  summary_fun = function(val) c(mean = mean(val)),
  win_rate = "CRPS",
  topK_rate = NULL,
  K = 5,
  effective_rate = NULL,
  tolerance = NULL,
  failure_rate = FALSE,
  soft_rel = "CRPS",
  epsilon = 0.001,
  group_by = NULL,
  split_tables = TRUE,
  ties_method = "min",
  latex = FALSE,
  ...
)
}
\arguments{
\item{obj}{A \code{duq_sim_study} object from \code{process_sim_study()}.}

\item{summarize}{Character vector of metrics to summarize via \code{summary_fun}.}

\item{summary_fun}{A function applied to each column in \code{summarize}.
Must return either a single value or a named vector. Names become suffixes
in the resulting columns.}

\item{win_rate}{Metrics for which to compute the proportion of rank-1 cases.}

\item{topK_rate}{Metrics for which to compute the proportion of rank <= K.}

\item{K}{Integer vector of K values for top-K rate (default = 5).}

\item{effective_rate}{Metrics for which to compute "good-enough" rates.}

\item{tolerance}{Numeric vector of tolerances (same length as effective_rate).}

\item{failure_rate}{Logical; if TRUE, include proportion of times
\code{failure_type != "none"}.}

\item{soft_rel}{Metrics for which to compute average soft-relative scores.}

\item{epsilon}{Numeric vector of epsilons (same length as soft_rel).}

\item{group_by}{Character vector of columns to split summaries by
(e.g. \code{c("n_train","NSR")}). Default = NULL (collapse across all scenarios).}

\item{split_tables}{Logical; if TRUE (default), return a list of tables
(one per group). If FALSE, return a single combined data.frame with
grouping columns retained.}

\item{ties_method}{Character vector for how to handle ties for ranking. Default "min".}

\item{latex}{Logical; if TRUE, return LaTeX tables via \code{stargazer}.
Requires \pkg{stargazer}.}

\item{...}{Additional arguments passed to \code{stargazer()}.}
}
\value{
A data.frame (if \code{split_tables = FALSE}) or a list of
  data.frames/LaTeX tables (if \code{split_tables = TRUE}).
}
\description{
Produces summary tables (by default, one row per method) with flexible
metrics such as averages, win rates, top-K rates, effective rates,
soft-relative averages, and failure rates.
}
